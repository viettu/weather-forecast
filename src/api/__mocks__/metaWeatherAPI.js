// import { mockLocations, mockWeathers } from "../metaWeatherAPI.data";

// export const getLocations = jest.fn((searchTerm) => {
//     return Promise.resolve(searchTerm ? mockLocations: []);
// });

// export const getWeathers = jest.fn((woeid) => {
//     return Promise.resolve(woeid ? mockWeathers : {});
// });
